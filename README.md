# COMP-3110 Line Mapping Project

This project implements a tool to track line mappings between two versions of a file.  
It also includes evaluation datasets, XML ground-truth mappings, and GUI design concepts.

## Project Features
- Compare two Java/text files line-by-line
- Detect unchanged, modified, inserted, and deleted lines
- Output a clean mapping of old → new line numbers
- (Upcoming) CSV output
- (Upcoming) Evaluation against XML ground truth.
